// Generated by CoffeeScript 2.3.2
(function() {
  var ImageElements;

  ImageElements = class ImageElements {
    constructor(images) {
      var alt, id, k, ref, ref1, src, v;
      this.div = document.createElement('div');
      this.div.setAttribute('id', 'images');
      this.html = '';
      ref = images.conference_logos_64;
      for (k in ref) {
        v = ref[k];
        id = `conference-logo-${k}`;
        src = `data:image/png;base64, ${v}`;
        alt = `logo for ${k}`;
        this.html += `<img id="${id}" src="${src}" alt="${alt}"> \n`;
      }
      ref1 = images.team_logos_64;
      for (k in ref1) {
        v = ref1[k];
        id = `team-logo-${k}`;
        src = `data:image/png;base64, ${v}`;
        alt = `logo for ${k}`;
        this.html += `<img id="${id}" src="${src}" alt="${alt}"> \n`;
      }
      this.div.innerHTML = this.html;
      document.body.appendChild(this.div);
    }

  };

  window.ImageElements = ImageElements;

}).call(this);
